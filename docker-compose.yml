version: '3.8'

services:
  bot:
    build: .
    container_name: binance-microbot
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
    networks:
      - bot-network

networks:
  bot-network:
    driver: bridge

